# Company deployment template for okr.mycompany.com
# Copy to deploy/docker/.env and replace values marked CHANGE_ME.

# Streamlit container ports
PORT=8501
HOST_PORT=8501

# Subdomain deployment (recommended): keep empty
# If you host at https://mycompany.com/okr then set BASE_URL_PATH=okr
BASE_URL_PATH=

# Production mode: required for enterprise deployments
PRODUCTION=true

# Managed PostgreSQL (recommended)
# IMPORTANT: if password has special characters, URL-encode it.
OKR_DATABASE_URL=postgresql+psycopg2://okr_app:CHANGE_ME@db.mycompany.internal:5432/okr

# Optional fallback alias
DATABASE_URL=

# Authentication throttling defaults
AUTH_USER_WINDOW_SECONDS=300
AUTH_USER_MAX_ATTEMPTS=5
AUTH_IP_WINDOW_SECONDS=300
AUTH_IP_MAX_ATTEMPTS=20
AUTH_LOCKOUT_SECONDS=900

# Optional fixed image tag (recommended for predictable rollback)
# IMAGE=ghcr.io/mycompany/okr-streamlit:2026-02-14

# Optional local Postgres container settings (used only with docker-compose.postgres.yml)
POSTGRES_DB=okr
POSTGRES_USER=okr
POSTGRES_PASSWORD=CHANGE_ME_LOCAL_ONLY
PG_HOST_PORT=5432
