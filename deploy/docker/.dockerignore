# Exclude everything by default
**

# Allow only what we need
!streamlit_app/**
!deploy/**

# But still ignore build outputs and caches inside
streamlit_app/__pycache__/
streamlit_app/.venv/
**/.mypy_cache/
**/.pytest_cache/
**/*.log
**/.DS_Store
