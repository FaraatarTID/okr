# IMPORTANT: This is a template. Generate real secrets before applying:
#   OKR_BACKEND_SERVICE_TOKEN=$(openssl rand -hex 32)
#   OKR_BACKEND_SIGNING_SECRET=$(openssl rand -hex 32)
apiVersion: v1
kind: Secret
metadata:
  name: okr-backend-auth
  namespace: okr
type: Opaque
stringData:
  OKR_BACKEND_SERVICE_TOKEN: ""
  OKR_BACKEND_SIGNING_SECRET: ""
