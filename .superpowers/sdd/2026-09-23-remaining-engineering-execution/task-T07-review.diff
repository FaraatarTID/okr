diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index 987ee2e..b8bfa8b 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -607,6 +607,18 @@ jobs:
       - name: Check workflow secret prerequisites
         run: python scripts/check_workflow_secret_prerequisites.py
 
+  repository-readiness:
+    name: Repository readiness contracts
+    runs-on: ubuntu-latest
+    timeout-minutes: 5
+    steps:
+      - name: Checkout
+        uses: actions/checkout@v6
+      - name: Verify observability and runbook contract
+        run: python -m scripts.verify_observability_readiness
+      - name: Verify OPS-01 artifact contract
+        run: python -m scripts.verify_ops01_readiness
+
   ci-result:
     name: CI result
     runs-on: ubuntu-latest
@@ -621,6 +633,7 @@ jobs:
       - contracts-quality
       - deployment-topology-quality
       - workflow-secret-prerequisites
+      - repository-readiness
     steps:
       - name: Require all executed checks to pass
         shell: bash
@@ -634,9 +647,10 @@ jobs:
           CONTRACTS_RESULT: ${{ needs.contracts-quality.result }}
           TOPOLOGY_RESULT: ${{ needs.deployment-topology-quality.result }}
           SECRET_PREREQ_RESULT: ${{ needs.workflow-secret-prerequisites.result }}
+          READINESS_RESULT: ${{ needs.repository-readiness.result }}
         run: |
           set -euo pipefail
-          for result in "$CHANGES_RESULT" "$MIGRATION_RESULT" "$BACKEND_RESULT" "$SPA_RESULT" "$E2E_RESULT" "$DOCS_RESULT" "$CONTRACTS_RESULT" "$TOPOLOGY_RESULT" "$SECRET_PREREQ_RESULT"; do
+          for result in "$CHANGES_RESULT" "$MIGRATION_RESULT" "$BACKEND_RESULT" "$SPA_RESULT" "$E2E_RESULT" "$DOCS_RESULT" "$CONTRACTS_RESULT" "$TOPOLOGY_RESULT" "$SECRET_PREREQ_RESULT" "$READINESS_RESULT"; do
             if [[ "$result" == "failure" || "$result" == "cancelled" ]]; then
               echo "CI result failed because a required job returned: $result"
               exit 1
diff --git a/tests/test_ops01_readiness.py b/tests/test_ops01_readiness.py
index 229b5e7..3575ea6 100644
--- a/tests/test_ops01_readiness.py
+++ b/tests/test_ops01_readiness.py
@@ -5,7 +5,7 @@ import sys
 
 def test_ops01_readiness_script_runs():
     result = subprocess.run(
-        [sys.executable, "scripts/verify_ops01_readiness.py"],
+        [sys.executable, "-m", "scripts.verify_ops01_readiness"],
         check=False,
         capture_output=True,
         text=True,
diff --git a/tests/test_repository_readiness_workflow.py b/tests/test_repository_readiness_workflow.py
new file mode 100644
index 0000000..bf6049c
--- /dev/null
+++ b/tests/test_repository_readiness_workflow.py
@@ -0,0 +1,115 @@
+"""Execute the repository-readiness workflow commands against good and bad inputs."""
+
+from __future__ import annotations
+
+import os
+import shlex
+import shutil
+import subprocess
+import sys
+from pathlib import Path
+
+import pytest
+import yaml
+
+
+ROOT = Path(__file__).resolve().parents[1]
+WORKFLOW = ROOT / ".github/workflows/ci.yml"
+
+CASES = (
+    (
+        "Verify observability and runbook contract",
+        "verify_observability_readiness",
+        ("docs/OBSERVABILITY_AND_RUNBOOKS.md",),
+        "docs/OBSERVABILITY_AND_RUNBOOKS.md",
+        "API Service Health Dashboard",
+        "OBSERVABILITY READINESS CHECK PASSED",
+    ),
+    (
+        "Verify OPS-01 artifact contract",
+        "verify_ops01_readiness",
+        (
+            "docs/OPS_READINESS_AND_RECOVERY_GUIDE.md",
+            "src/database.py",
+            "backend_app/jobs.py",
+            "backend_app/worker.py",
+            "backend_app/config.py",
+            "backend_app/routers/platform_routes.py",
+            "alembic/versions/baseline_2026_08_26_schema.py",
+        ),
+        "docs/OPS_READINESS_AND_RECOVERY_GUIDE.md",
+        "Retention and Table-Growth Control Policy",
+        "OPS-01 readiness verification passed.",
+    ),
+)
+
+
+def _workflow_command(step_name: str) -> str:
+    workflow = yaml.load(WORKFLOW.read_text(encoding="utf-8"), Loader=yaml.BaseLoader)
+    jobs = workflow["jobs"]
+    assert "repository-readiness" in jobs["ci-result"]["needs"]
+    assert "if" not in jobs["repository-readiness"]
+    result_step = jobs["ci-result"]["steps"][0]
+    assert (
+        result_step["env"]["READINESS_RESULT"]
+        == "${{ needs.repository-readiness.result }}"
+    )
+    assert '"$READINESS_RESULT"' in result_step["run"]
+    steps = jobs["repository-readiness"]["steps"]
+    commands = [step["run"] for step in steps if step.get("name") == step_name]
+    assert len(commands) == 1
+    assert "continue-on-error" not in next(
+        step for step in steps if step.get("name") == step_name
+    )
+    return commands[0]
+
+
+@pytest.mark.parametrize(
+    ("step_name", "module", "files", "damaged_file", "marker", "success_message"),
+    CASES,
+)
+def test_readiness_workflow_runs_good_and_bad_fixtures(
+    tmp_path: Path,
+    step_name: str,
+    module: str,
+    files: tuple[str, ...],
+    damaged_file: str,
+    marker: str,
+    success_message: str,
+) -> None:
+    command = _workflow_command(step_name)
+    assert command == f"python -m scripts.{module}"
+    for relative in files:
+        target = tmp_path / relative
+        target.parent.mkdir(parents=True, exist_ok=True)
+        shutil.copyfile(ROOT / relative, target)
+
+    env = os.environ.copy()
+    env["PYTHONPATH"] = os.pathsep.join(
+        part for part in (str(ROOT), env.get("PYTHONPATH", "")) if part
+    )
+    args = shlex.split(command)
+
+    def run_gate() -> subprocess.CompletedProcess[str]:
+        return subprocess.run(
+            [sys.executable, *args[1:]],
+            cwd=tmp_path,
+            env=env,
+            capture_output=True,
+            text=True,
+            check=False,
+        )
+
+    good = run_gate()
+    assert good.returncode == 0, good.stdout + good.stderr
+    assert success_message in good.stdout
+
+    broken = tmp_path / damaged_file
+    content = broken.read_text(encoding="utf-8")
+    assert marker in content
+    broken.write_text(
+        content.replace(marker, "removed-for-negative-fixture", 1), encoding="utf-8"
+    )
+    bad = run_gate()
+    assert bad.returncode != 0, bad.stdout + bad.stderr
+    assert marker in bad.stdout
