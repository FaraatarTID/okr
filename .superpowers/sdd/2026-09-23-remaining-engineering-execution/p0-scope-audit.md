# Phase 1 P0 scope audit (read only, 2026-09-23)

Source: `docs/REMAINING_ENGINEERING_PLAN.md:195-237`, especially the Phase 1 Progress rows at lines 230-237. This is the live status register: `docs/architecture-status.md:6-19` calls itself historical and explicitly distinguishes its `P0-00`…`P0-06` IDs from the register's `P0-1`…`P0-8`. The active execution plan says the register is authoritative, but its 32-packet T00–T31 map names no P0 row. T00/T01's A–F-only scope consequently omits these live items. No test or runtime check was run for this audit.

| Register item and current status | Open acceptance and evidence needed | Current repo evidence | Best packet placement / dependency |
| --- | --- | --- | --- |
| **P0-2 — Open** | Settle the intended task visibility predicate, then make TCP and Supabase API return the same task rows for owner-visible and assignee-visible cases. Remove the broad `except Exception: continue` path that silently drops a task; show a real failure or a bounded, explicit outcome rather than losing rows. Use behavioral parity cases for a task assigned to an in-scope user on an out-of-scope goal, plus failure injection for attribute/query errors. The row expressly says the right predicate depends on the path-parity decision; an implementer must not silently decide a product visibility change. | `backend_app/response_scope_helpers.py:469-494` accepts owning goal **or** assignee and catches every exception. `src/services/supabase_api_mode_read.py:564-650` selects only tasks under allowed goal IDs, including its fallback. `tests/test_dual_mode_parity.py` has a `tasks.by_cycle` kind and out-of-scope cycle cases, but the row remains open for payload parity. F1's acceptance at `docs/REMAINING_ENGINEERING_PLAN.md:183` itself says six-kind payload parity is unmet. | Add P0-2 explicitly to **T02** (F1 payload parity), or give it an immediately adjacent packet sharing that same backend read-path lane. Establish the visibility decision before changing either predicate. Run after T03 if actor-presence changes affect the same paths; serialize both as the progress ledger already requires. |
| **P0-3 — Open** | Measure `ritual.snapshot` over a real PostgreSQL-backed path, including the TCP fan-out and the HTTPS RPC-missing fallback; assert that P0-1's request-local scope cache actually collapses repeated scope resolution and record statement/physical-connection counts. A test that skips in CI or runs only against SQLite is not acceptance. The current SQLite attempt returned HTTP 500 because `fn_ritual_snapshot` does not exist there. | `backend_app/read_query_helpers.py:386-520` validates `weekly_plan.active` and recursively invokes five sub-kinds; `tests/test_read_path_budget.py:480-493` explicitly excludes this kind. `tests/test_read_path_budget_postgres.py:1-170` provides the real PostgreSQL fixture/counters for `krs.by_cycle`, and `.github/workflows/ci.yml:181-186` provisions PostgreSQL to prevent a permanent skip. `tests/test_ritual_snapshot_rpc.py` exercises RPC/fallback behavior but is not the budget. | Give P0-3 a **dedicated backend performance packet** after T02/T03 read-path changes and before any claim of backend read-path completion; reuse the PostgreSQL harness but handle the function/fallback setup explicitly. T13 is a frontend page-load packet and does not cover this. |
| **P0-4 — In-repository complete; deployment not verified** | Add the missing SPA-web → BFF behavior test that proves `X-OKR-Client-IP` crosses that hop while untrusted forwarding headers cannot choose the key. Separately, demonstrate on the deployed network path that requests can reach the BFF only through the edge that overwrites the private header. Keep the documented trust decision; this row says it is already decided and implemented. Do not call local/CI tests deployment proof. | `docs/client-ip-trust-adr.md`; `deploy/nginx.conf:23` overwrites the private header; `spa-web/src/lib/bff-proxy.ts:91-103` forwards it; `spa-bff/src/proxy.ts:103-123` sends it onward; `backend_app/security.py:223-230` honors it only with a valid service token. `spa-web/src/lib/bff-proxy.test.ts` has five log-redaction/error cases and no forwarding assertion. `spa-bff/test/forwarded_ip.test.ts`, `tests/test_rate_limit_key_trust.py`, and `tests/test_login_throttle_key.py` cover later boundaries. | Put the missing SPA-web boundary test in an **early small P0-4 packet before T24** uses the trusted address for D4 auth limiting. Put the deployment-only closure in the **T30/T31 provider deployment lane**, conditioned on a selected target/ingress and recorded reachability evidence. Its repository subtask may close while the register row remains deployment-unverified. |
| **P0-5 — Open, one remaining member** | Resolve the two green, caller-less identity controls by wiring them into a real supported production path **if** the approved identity design needs them, or remove/relabel the inert control and claims. Evidence must trace production callers and exercise the real request/lifecycle path, with a negative case so a dormant control cannot yield green. The row says the rest of the sweep is closed; Python-only sweep limits and declarative checks are recorded scope limits, not closure blockers. Do not re-open P0-6, D3a, or already closed examples as part of this packet. | `spa-bff/src/session.ts:136` exports `revokeSessionsForIdentity`, imported only by `spa-bff/test/identity_session_revocation.test.ts`; `src/saas/identity_ports.py` describes provider-neutral and test-only implementations, and search found only `tests/test_saas_identity_ports.py` importing it. `docs/REMAINING_ENGINEERING_PLAN.md:234` states this exact open disposition and lists closed examples/limits. | Add an **explicit P0-5 identity-control disposition packet** tied to T19 (provisioned identity), T22/T23 (D3 owner decision and implementation), and T25 (customer identity expansion gate). It cannot assert that the Python mock port or BFF helper is a production control merely because their tests pass. If the T22 owner decision selects account-wide semantics, reconcile/remove any per-session facade accordingly; if per-session semantics stay required, prove the production caller after T23. |
| **P0-8 — Open** | Keep PostgreSQL `NullPool` as default until a PgBouncer container in **transaction pooling** mode sits between the read-path budget harness and PostgreSQL, with before/after statement, checkout, physical-connection, and behavior results. Exercise both default and `OKR_DB_USE_NULL_POOL=false` branches and preserve the server-side-cursor/session-state tripwires. Only after that evidence may the one-line default flip be considered; a direct-PostgreSQL CI green is insufficient. The row does not make the prepared-statement concern an active psycopg2 control. | `src/database.py:147-184` defaults `OKR_DB_USE_NULL_POOL` to true and has a functional pooled opt-in. `tests/test_read_path_budget_postgres.py:220-305` measures direct PostgreSQL `NullPool` versus `QueuePool` and guards cursor behavior, but its own comments say this does not verify PgBouncer. `.github/workflows/ci.yml:181-186` has a PostgreSQL service, not a pooler. `docs/CONFIG_REFERENCE.md:54-55` documents current default. | Give P0-8 a **separate pooler verification packet** after the PostgreSQL read-path harness work (P0-3 is a natural precursor). Run against an explicitly configured transaction-pooler hop in CI or a reproducible local container with retained evidence. Keep the default unchanged while blocked; do not merge this into T13's browser budget or T29's provider adapter work. |

## Execution-plan changes implied

The smallest coherent map is: broaden T00/T01 to reconcile Phase 1 P0 rows as well as A–F; broaden T02 to name P0-2 and its visibility-decision gate; add dedicated packets for P0-3, P0-4's missing repository test/deployment gate, P0-5's identity-control disposition, and P0-8's PgBouncer verification. Update the declared packet count, register-to-packet map, ordered waves, conflict matrix, and SDD checklist together. P0-4's deployment result remains external to repository-only acceptance; P0-5 must wait on the identity architecture choice; P0-8 must not become a default change before the transaction-pooler measurement.
