# Architecture Status Ledger

Documentation HQ: [README](../README.md)

| Item | Status | Evidence | Verified | Retro note |
|---|---|---|---|---|
| P0-00 - Architecture inventory and target topology | VERIFIED | [pre-saas-architecture-inventory.md](pre-saas-architecture-inventory.md); [architecture-boundaries.md](architecture-boundaries.md) | 2026-09-02 | Runtime inventory and target service topology are recorded; the promoted pre-SaaS baseline uses the canonical backend, BFF, SPA, API, and worker boundaries. |
| P0-01 - Canonical backend package and facade boundary | VERIFIED | [architecture-boundaries.md](architecture-boundaries.md); canonical serializers, bucket, selector, bootstrap delegation, keyed/unkeyed snapshot-cache factories, all facade snapshot-cache wiring, duplicate implementation removal, and root `app.py` retirement completed; repository boundary/design gates passed | 2026-09-02 | No supported root facade callers remain. Provider deployment evidence is tracked separately under P0-02/P0-03/P0-06. |
| P0-02 - Runtime and deployment entrypoint canonicalization | VERIFIED repository-side | [runtime-entrypoint-contract.md](runtime-entrypoint-contract.md); runtime matrix, compatibility readiness gate, isolated SaaS `database` readiness smoke, live compatibility health payload, and deployment topology checks passed | 2026-09-02 | Canonical service entrypoints and local topology are verified. Provider-specific ingress, restart, and live health evidence remain open. |
| P0-03 - BFF responsibility and topology ADR | VERIFIED repository-side; operational closure OPEN | [bff-boundary-adr.md](bff-boundary-adr.md); [bff-security-review.md](bff-security-review.md); import/package boundary checks passed; allowlist passed for 44 routes; BFF suite passed 65 tests; backend ingress regression covers signed-service-token enforcement and forwarded role-claim validation against the resolved actor scope; consolidated gate passed | 2026-09-14 | Repository security and dependency boundaries are verified. Darkube deployment, production measurements, rate-limit behavior under the provider, and paired rollback rehearsal remain open. |
| P0-04 - Root script and compatibility surface cleanup | VERIFIED | [compatibility-surface-cleanup.md](compatibility-surface-cleanup.md); [compatibility-callers.md](compatibility-callers.md); [launcher-command-matrix.md](launcher-command-matrix.md); root batch launchers removed, canonical Windows launchers retained under `scripts/windows/`, root `app.py` retired, all known callers migrated, and import guards/tests passed | 2026-09-02 | Provider-specific start/stop rehearsal remains an operational follow-up, not an unresolved repository compatibility surface. |
| P0-05 - Documentation consolidation and lifecycle control | VERIFIED | [documentation-lifecycle-control.md](documentation-lifecycle-control.md); `python scripts/check_docs_hq_links.py` passed across 93 Markdown files after the signed-review regression repair and topology-evidence additions | 2026-09-14 | Documentation control re-verified after the P2/P3 evidence additions; future ADRs must preserve the same ledger and link discipline. |
| P0-06 - Governance, migration safety, and exit review | OPEN for production evidence | [governance-migration-exit-review.md](governance-migration-exit-review.md); [migration-rollback-runbook.md](migration-rollback-runbook.md); provider backup/restore contracts, release-manifest validation, rollback-evidence validation, complete persisted operation status, and focused local tests passed | 2026-09-02 | Repository governance and release-evidence controls are verified. Production provider selection, provider-backed backup/restore, measured RPO/RTO, application rollback rehearsal, and accountable operational ownership remain required before real customer data. |
| SaaS Phase 0 - Environment contract | VERIFIED | [customer-environment-contract.md](saas/customer-environment-contract.md); typed `EnvironmentManifest`, explicit lifecycle transition table, isolated database-target validation, retired-state rejection tests, local provisioning, release operations, and metadata-only control-plane evidence | 2026-09-02 | The dedicated single-tenant contract and local lifecycle controls are complete. Production provider evidence, customer-data recovery, and operational ownership remain gated; shared-database tenancy/RLS is permanently out of scope. |
| Repository performance diagnostics | OPEN for end-to-end evidence | [performance.md](architecture/performance.md); `scripts/perf_hotpaths.py` benchmarked representative hot paths; `scripts/diagnose_page_load.py` attributes browser/BFF/backend/database/serialization trace time and reports missing instrumentation | 2026-09-02 | Local hot paths are fast and diagnostic tooling is repeatable, but the reported ~10-second page load is not closed until a reachable deployed stack produces a browser waterfall and layer timings. No speculative query fix is claimed. |
| Authentication recovery and password-change flow | VERIFIED repository-side | [CONFIG_REFERENCE.md](CONFIG_REFERENCE.md); bootstrap admin recovery and forced first-login password change are implemented and covered by application/UI checks | 2026-09-02 | The pre-SaaS recovery path is available for the empty synthetic database. Production credential provisioning and secret ownership remain deployment responsibilities. |
| A1 - Operator CLI interpreter bootstrap guard | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `scripts/deploy_saas_release.py` carries the same `if __package__ in {None, ""}` guard as the other four operator CLIs; `tests/test_cli_entrypoint_bootstrap.py` drives all five entrypoints through a subprocess so the guard cannot silently regress | 2026-09-20 | The fifth CLI was the only one missing the guard, and `uv run` from the repository root masked it by making `src` importable regardless. The regression test asserts the parser is reached from an unrelated working directory, not that a specific error is absent. |
| A2 - Restore path reachable through registration | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `register-target` subcommand added to `scripts/restore_saas_environment.py`; `validate_restore_target()` shared by registration and restore so an unsafe target cannot be persisted; CLI-driven provision, backup, register, and restore round trip covered across separate processes | 2026-09-20 | Registration previously applied no production-name guard, so a `live`-named target could be persisted even though restoring into it would later fail. The guard now runs at both points, and an unregistered target still fails closed. |
| C9 - Forced password change enforced at the shell boundary | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); enforcement in `spa-web/src/components/atlas-shell/useShellAccessControl.ts`, the bypassing redirect removed from `spa-web/src/app/login/page.tsx`, with helper and boundary tests | 2026-09-20 | The gate now holds for every authenticated route instead of depending on the login page's redirect. Existing shell access-control and auth-API tests were left unmodified and are green in CI. |
| C8 - Single shell mount across route navigation | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); the eight shell routes moved into the `spa-web/src/app/(shell)/` route group whose `layout.tsx` mounts `AtlasShell` once and whose pages render `null`; `modeForLocation()` in `atlas-shell/navigation.ts` reconciles the mode on path change; `spa-web/src/app/route-ownership.test.ts` is a static guard and `(shell)/layout.test.tsx` asserts the same DOM node survives navigation | 2026-09-20 | The defect was that each route file mounted its own shell, so every navigation re-ran the boot sequence and discarded all shell state. `spa-e2e` is green for this change, but the end-to-end drill remains open: on a warmed stack, navigate between modes and confirm no duplicate session, cycle, or team fetch and that the 45-second and 600-second snapshot polls are not restarted on same-mode navigation. The three-push CI history is recorded in the worklog. |
| C1 - Short-TTL cache for shell read datasets | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `spa-web/src/lib/resourceCache.ts` (TTL, in-flight joining, failures never cached), `spa-web/src/lib/cycles.ts` and `adminResources.ts` as cached merged reads, sign-out and database restore clear the whole cache in `useShellAccessControl.ts` and `useAdminActions.ts`, and mutation paths bypass and re-seed | 2026-09-20 | `cycles.all` was requested from three places, not the two the plan recorded, so a single mount cost five read requests before the snapshot started; it is now two. The session is deliberately **not** cached: it carries `role` and the gate decides what chrome renders, so caching it would widen the window in which a server-side demotion still shows admin controls. Invalidation is deliberately coarse rather than per-namespace: targeted mutations read with `bypassCache` and write the fresh result back, so the entry is re-seeded rather than merely emptied, and the only whole-cache callers are sign-out and restore. The drill remains open — navigate between modes on a warmed stack and count fetches. |
| C4 - Lint gate and `spa-bff` typecheck | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `eslint.config.mjs`, root `npm run lint`, `just lint`, and the `JavaScript lint gate` step in `spa-quality` | 2026-09-20 | Both halves are done, so `just lint` and `just typecheck` now cover both JS packages. The lint gate runs over 145 files with 0 errors and 37 warnings: `no-explicit-any` and `no-floating-promises` are `error`, while `no-unused-vars` and `exhaustive-deps` stay `warn` and their triage is tracked as F3 in the plan rather than counted as cleared. The script passes both whole packages and the config's ignores decide what is skipped, so a file added tomorrow is linted by default; an earlier hand-maintained path list had silently missed six files. The five error-level sites were fixed rather than downgraded, and the regex and `cause` changes were proven behaviour-preserving against 22 inputs and a non-enumerability check before landing. |
| C4 - vitest 5 realignment | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `spa-bff/tsconfig.build.json`, `spa-bff/vitest.config.ts`, `spa-bff/Dockerfile`, `spa-bff/package-lock.json` | 2026-09-20 | Follows the kept `vitest` 3.x to 5.0.1 bump. `spa-bff/package-lock.json` was regenerated because it still declared `^3.2.4`, which made the dependency licence gate audit a tree that did not match its manifest. Separately, vitest's default test `exclude` shrank in the 4.x line to only `node_modules` and `.git`, and since `tsconfig.json` compiled `test/**/*.ts` into `dist/test/`, a tree with a build present ran every test twice and the slowest timed out at 5s; CI was protected only by running tests before the build. Fixed at the root with a `tsconfig.build.json` that emits only `src`, so tests can no longer enter `dist`, plus the Dockerfile copy and CI cache-key hash that change requires, and a `vitest.config.ts` restoring the documented pre-4.x exclude list. It is recorded as an open question in the plan that `engines.node` still declares `>=20` while vitest 5 needs `>=22.12.0`. |
| C4 - build emit correctness | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `spa-bff/tsconfig.build.json` | 2026-09-20 | Pre-existing defect found while checking the build change was safe, and fixed at the root. `tsc --incremental` trusts its `.tsbuildinfo` and never checks that outputs exist, so with a build-info present and `dist` deleted it exits 0 having emitted nothing; the original `tsconfig.json` build behaves the same, so this predates the vitest work. Because `spa-build-cache` restores `spa-bff/.cache` but not `spa-bff/dist`, `SPA BFF build` could report success while producing no artifact, and no step verified the artifact existed. The container is unaffected — the Dockerfile never copies `.cache`, so the image build always starts cold, and the Container Image Build Gate builds it from a fresh context. Fixed by making the emitting build non-incremental, proven adversarially with a planted stale build-info whose `dist` had been deleted. |
| B6 - Quality-gate baseline expiry resolved | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `pyproject.toml` (`[tool.ruff] extend-exclude`), `.pre-commit-config.yaml` (`ruff-format-check-repo`), `.github/workflows/ci.yml` (`Format Check (Ruff, repo-wide)`), `scripts/check_quality_gate_baseline.py`, `docs/QUALITY_GATE_BASELINE.md` | 2026-09-20 | QG-001 and QG-002 both expired 2026-09-30, and `check_quality_gate_baseline.py` is an `always_run` pre-commit hook as well as a CI gate, so from 2026-10-01 every commit and every CI run would have failed. QG-001 was closed rather than deferred: the format check went from two named files to repo scope, `ruff format` reformatted 170 files, and `ruff format --check .` now passes over 348. It had been enforced only by pre-commit — CI ran `ruff check` but never `ruff format` — so a CI step was added as well. `alembic/versions` is excluded through `[tool.ruff] extend-exclude` because an applied migration must stay byte-stable; that is the only path carve-out, and the frozen baseline file's SHA-256 was verified identical before and after the reformat. QG-002 was re-dated to 2026-11-15 with its debt measured and recorded (127 mypy errors in 24 of 347 files) and a staged burn-down to 80, then 40, then 0 errors. Re-dating both items was rejected as masking the debt. Four gates that inspect a live schema — `check_insert_contract`, `check_rls_enabled`, `verify_migration_sequence` and `verify_postgresql_integration` — cannot run locally without `OKR_DATABASE_URL`, so they are not verifiable in this working copy; all four passed in `backend-quality` on `f391c7b`, where CI supplies a postgres service, along with the new repo-wide format step. |
| A5 - Attestation signature verification | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `scripts/attestation_verification.py` (new shared verifier), `scripts/verify_recovery_evidence.py`, `scripts/verify_rollback_evidence.py`, `scripts/check_saas_phase1_evidence.py`, `tests/test_attestation_verification.py` (new), `pyproject.toml`, `backend_app/requirements.txt`, `uv.lock`, [CONFIG_REFERENCE.md](CONFIG_REFERENCE.md) | 2026-09-20 | The previous check was self-consistent by construction: it required `signed_payload_sha256` to equal a digest recomputed from the same file, so a fabricated constant signature passed. The proof is that three existing tests asserted a *valid* attestation using the literal string `provider-signature-value-with-more-than-32-bytes`, and they passed; after the fix they failed until the fixtures signed with a real key. Verification now runs against configured key material for `provider-signed` (HMAC-SHA256 over the canonical evidence with `attestation` removed) and for `ed25519` and `rsa-pss-sha256` (PEM public key, `<algorithm>:<base64>`). Every path fails closed — absent secret, absent public key, unsupported algorithm, missing prefix, invalid base64, and mismatch all raise — and the constant-time comparison is centralised so no caller can compare signatures with `==`. `cryptography==50.0.1` was installed but undeclared and unused in-repo; it is now an explicit dependency rather than a transitive accident. New tests cover a fabricated signature with a valid self-digest, a payload tampered with after signing, a missing secret, a wrong key, a key type mismatch, and malformed signatures. |
| B1 - Backup-evidence doc schema contradicted its own verifier | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); [hamravesh-backup-onboarding.md](saas/hamravesh-backup-onboarding.md), `tests/test_documented_evidence_schema.py` (new) | 2026-09-20 | The guide documented `backup.id` and omitted `created_at`, `checksum_payload`, and `attestation`, so an operator who followed it exactly could never pass verification; the omission was invisible because no test read the document. The example now matches the verifier, including the `checksum_payload` binding that ties the checksum to the database identity, environment, and creation time, with operator-supplied values left as angle-bracketed placeholders. The conformance test fills those placeholders the way the document instructs and asserts the real verifier accepts the result, so drift fails CI. The same class was closed for `docs/saas/phase-1-entry-evidence.md`, which is also machine-read: a probe confirmed the checker emits distinguishable errors for a missing top-level member (`missing structured evidence field:`) and a missing attestation member (`missing attestation field:`), so the test proves the document names every member both sets require. The guarantee is bounded and stated in the test: a renamed member of `decision`, `provisioning`, `release`, `backup`, `restore`, `rpo_rto`, or `owners` is indistinguishable from an unrecorded fact, because the checker reports both with the same message. |
| B3 - Documented behaviour diverged from the code | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); [bff-boundary-adr.md](bff-boundary-adr.md), [bff-security-review.md](bff-security-review.md), [evidence/security-parity.json](evidence/security-parity.json), [topology-review.json](topology-review.json), `docs/security/rate-limit.json`, `docs/security/csrf-origin.json`, `spa-bff/README.md` | 2026-09-20 | The gap was wider than the row recorded, and narrower in one place. CSRF protection does exist — double-submit cookie validated against `X-XSRF-TOKEN`, 403 `INVALID_CSRF_TOKEN` on mismatch — so the ADR's "CSRF and origin controls" row was half true; the row conflated two mechanisms, which is why the fix splits the matrix instead of deleting a line. Origin validation and BFF-side rate limiting do not exist. The more serious finding is that the evidence layer, not just prose, asserted `passed` for both controls: `security-parity.json` claimed `rate_limiting` passed while its own artifact described session fail-closed behaviour, and claimed `csrf_and_origin_controls` passed while describing CSRF alone. Both are now `pending` with the observed behaviour stated, and both validators fail closed on that — the correct outcome, matching F6's precedent rather than being treated as breakage. `validate_security_parity` structurally requires every control to be `passed`, which is *why* the mislabelling happened; the `pending` status the templates already use is representable, so no validator change was needed to tell the truth. Both validators do fail closed when invoked the way this repository documents — `python -m scripts.validate_security_parity docs/evidence/security-parity.json` and `python -m scripts.validate_topology_review docs/topology-review.json`, per `docs/BFF_RUNTIME_SMOKE_CHECKLIST.md` — and the direct-script form fails with `ModuleNotFoundError: No module named 'scripts'` because both import siblings as `scripts.*`, which is the documented reason F8 says to use `-m`. That was checked against the convention before any change was considered, so no code was altered for it. Nothing invokes either validator automatically, and F8 already records both as absent from every enforcement surface, so this fail-closed behaviour is a manual check today rather than a CI gate. |
| B5 - RLS gate name implied rejected architecture | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `.github/workflows/ci.yml` (job id, step name, `ci-result` needs and result variable), `scripts/check_rls_enabled.py`, [architecture/ARCHITECTURE.md](architecture/ARCHITECTURE.md), [DOCUMENTATION_LIFECYCLE.md](DOCUMENTATION_LIFECYCLE.md) | 2026-09-20 | The check was always legitimate; only its name and surrounding prose misled. It is PostgREST exposure hardening — an unsecured table in the `public` schema is reachable through the generated REST API with the anon key — and it is unrelated to shared-database tenant isolation, which ADR-001 permanently rejects. The script now says so in its own header, and ARCHITECTURE.md states the distinction where a reader has just been told RLS is out of scope, which is exactly where the confusion arose. Renaming the job id renames the GitHub status check, so a branch-protection rule referencing `rls-gate` needs updating; that consequence is documented rather than left for someone to discover, and no test asserted the old id (19 CI-contract tests pass unchanged). |
| B7 - Repository memory gaps | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `docs/WORKLOG.md` (gitignored local memory), commits `25c984d` and `1016d81` | 2026-09-20 | The unmerged-change-set half was already resolved before this item started: no file B7 listed is uncommitted, so only the worklog half needed work. The worklog had no 2026-09-14 entry at all, so two commits were absent from repository memory. The entries are **reconstructed from the commits** and labelled that way, because I did not author that work and will not present it as a first-hand session record; each claim was checked against the committed code rather than inferred from the subject line. That check is what makes the entries useful: `1016d81` is the origin of the process-local revocation registry that D3 tracks, and `25c984d` already contained the token pair D6-1 has since deprecated, so the open D items are known edges of a landed surface rather than unfinished work. |
| B8 - Declared surfaces were inert | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `spa-web/src/components/atlas-shell/AdminModePanel.tsx`, `shellAnalyticsUtils.ts`, `shellAnalyticsUtils.test.ts`, `docs/CONFIG_REFERENCE.md`, `docs/CONFIG_REFERENCE_FA.md`, `spa-web/README.md`, `DEPLOYMENT.md`, `deploy/docker/docker-compose.yml` | 2026-09-20 | Two dispositions, both by removal, plus one half that was already fixed. `AdminTab`'s `security` member existed only in the type union — no tab entry, no render branch — and is gone. The `NEXT_PUBLIC_OKR_AI_SYNC_*` pair and the `aiProgressDecision` function they fed are gone from all five documentation and deployment sites. Implementation was rejected for a concrete reason, not out of caution: `ai_overall_score` is derived backend-side from `ai_analysis` (`src/domain/read_queries.py:40`), so the client never holds the proposed score a cap would need, and wiring one would mean reimplementing the backend derivation in TypeScript or inventing a second definition of it. The `rtlStyle` import in `RitualModePanel.tsx` was already removed before this item began; that half of the row was stale rather than unfixed, and it is recorded as such. spa-web stays at 47 files and 229 tests. |
| C7 - `spa-bff` local test setup collected duplicates | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `spa-bff/vitest.config.ts`, `spa-bff/tsconfig.build.json` (both committed in `c1ad642`) | 2026-09-20 | Already closed by the C4 vitest realignment, so this item verified rather than redid it. `vitest.config.ts` restores the pre-4.x exclude list including `**/dist/**`, and `tsconfig.build.json` keeps tests out of `dist` entirely, so correctness no longer depends on build ordering. The criterion was proven adversarially instead of inferred from a green run: with `spa-bff/dist/test/session.test.ts` planted, the suite still collected exactly 9 files and 74 tests. That matters because a passing count on a tree with no `dist` would have proved nothing. The planted file was removed and the build artifact the probe deleted was rebuilt to its original 7 files including `dist/src/server.js`. |
| A3 - The mandatory Phase 1 promotion gate was never executed | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `.github/workflows/promote-production.yml` (new `phase-1-evidence` job), `tests/test_phase1_promotion_gate.py` | 2026-09-20 | `scripts/check_saas_phase1_evidence.py` was reachable only from a `just` recipe, so the gate `docs/architecture/ENTERPRISE_SAAS_ROADMAP.md` calls mandatory was not executed by any enforcement surface. The row's own premise needed correcting before it could be finished: it claimed `verify_rollback_evidence.py` had no CI coverage, but that script is invoked three times in `rollback-production.yml` — those invocations simply cannot succeed, which is A6, so wiring it again would have duplicated a broken call site rather than covered an uncovered gate. The fixture half was already satisfied, and that was verified rather than assumed: every verifier has pytest coverage and `backend-quality` runs the full `pytest -q` suite, so the tests asserting that a forged signature is rejected already gate each pull request. What was genuinely missing was the real-bundle gate and its placement. It is now its own job, `phase-1-evidence`, in `promote-production.yml`, listed in the `needs` of the `production`-environment approval job, so promotion cannot proceed while the bundle is incomplete. It is deliberately **not** a pull-request gate. The bundle honestly records `BLOCKED`, `UNSELECTED` and `real_data_approval: false`, so a required per-PR check would be permanently red, and a permanently red required check teaches people to ignore required checks; the gate's own fail-closed output would then mean nothing. `tests/test_phase1_promotion_gate.py` pins the placement and asserts the negative — that the real-bundle gate is absent from `ci.yml` — so it cannot drift back to being unreachable. Run by hand the gate still exits 1 with six honest errors, which is F6's subject and is the correct behaviour rather than a defect. |
| F1-F3 - Retry timeout, proxy diagnostics, lint triage | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `spa-web/src/lib/api/http.ts`, `spa-web/src/lib/bff-proxy.ts`, `useSnapshotLifecycle.ts`, `useInspectorNodeActions.ts`, `useAdminActions.ts` | 2026-09-20 | F1: the exported `perAttemptTimeoutMs` option on `retryWithFetch` was never read, so it advertised a deadline it did not implement. The deadline now lives in the helper through an `AbortSignal`, both call sites forward it and keep their real 120s budget, and the redundant `fetchWithTimeout` is gone. The first write-up of F1 also claimed a hung request could hold a retry slot indefinitely; that was wrong, because the call sites already time-boxed each attempt at 120s, and the claim is withdrawn rather than quietly edited. F2: the BFF proxy discarded its error, so a request that never reached the BFF left no trace on either side; it now emits one structured JSON line matching the BFF's shape, logging the pathname and never headers, cookies or the body, with a test asserting a cookie, an authorization header and a query value do not appear in the output. F3: lint warnings went from 37 to 8 and all 18 `exhaustive-deps` reports are resolved, two of which were real bugs — a stale `mode` in the snapshot callback that broke `include_analysis` in both directions, and a save path missing two draft fields it writes for key results. The 8 remaining warnings are unfinished-wiring findings, left visible rather than deleted. |
| F4-F7 - Gate sweep: silent npm scan, fastify advisories, SaaS evidence gate, stale typecheck artifacts | F4 IMPLEMENTED, F5 Open (blocked), F6 Open, F7 IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `scripts/verify_dependency_scans.py` | 2026-09-20 | Running every gate, not only the JavaScript ones, found four more issues. F4: the dependency vulnerability scan resolved `shutil.which("npm")` but invoked the bare name `"npm"`, which `subprocess` cannot launch on Windows because the launcher is `npm.cmd` and `PATHEXT` is not applied; the `[WinError 2]` was reported only as a warning, so the scan exited 0 having audited nothing. Now fixed to invoke the resolved path and verified by probing both forms. F5: `fastify@5.8.5` carries two moderate advisories patched in 5.12.1 (latest 5.12.5), one of them `X-Forwarded-*` spoofing under `trustProxy`, which is directly relevant here. The upgrade verified clean in isolation (74 tests, build, typecheck) but could not be committed because the root workspace lockfile refused to re-resolve its fastify entry under a declared `^5.12.5`, which would have left `npm ci` inconsistent; the attempt was reverted and the tree is clean. F6: the SaaS Phase 1 evidence gate fails closed as designed, because the evidence document honestly records `BLOCKED`/`UNSELECTED`, and it is not wired into CI; it must not be satisfied by inventing the production facts it demands. F7: the eight local `TS2307` errors were stale `.next` generated artifacts, repaired by rebuilding rather than by deleting the directory, and the typecheck is now zero-error with the artifacts present. |
| F8 - Twenty-five scripts are not invoked by any enforcement surface; fourteen are referenced nowhere at all | Open | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md) | 2026-09-20 | Measured against the three enforcement surfaces only — every workflow, the justfile and `.pre-commit-config.yaml` — and deliberately not against documentation. Twenty-five of seventy-four scripts are not invoked by any of them; eleven of those are imported by another script and are therefore live code, and fourteen are neither invoked nor imported. Several of the fourteen are verification gates that pass when run by hand — `python -m scripts.verify_observability_readiness` prints `OBSERVABILITY READINESS CHECK PASSED` — and that nothing runs, so they cannot fail and nothing is checked. This is the same family as F4: green because it does not run. Left open rather than wired up blind, because adding new required checks to CI is a policy change. **Correction (2026-09-20):** the first version of this row claimed ten unreferenced scripts and named `verify_helper_integrity` and `verify_module_export_contracts` as unenforced verification gates; both are in fact invoked in CI, so that claim was false and is withdrawn. The cause was a threshold bug that treated a stem appearing exactly once as appearing only in its own file, compounded by counting this document as evidence. Run these via `python -m scripts.X`; several import siblings as `scripts.*`, which the direct-script form breaks. |
| A6 - The production rollback workflow's evidence steps cannot pass | IMPLEMENTED | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `.github/workflows/rollback-production.yml:68-76,122-130`, `scripts/create_release_manifest.py:54-60`, `scripts/verify_rollback_evidence.py:214-218` | 2026-09-20 | Found while checking A5's blast radius rather than by looking for it. Both rollback evidence steps invoke `python scripts/verify_rollback_evidence.py --manifest release-manifest/release-manifest.json --commit-sha "$RELEASE_SHA" --repository "$REPOSITORY" --output ...` with no `--cosign-reference` and no attestation key material in the step environment. `verify_rollback_manifest` requires signed Cosign references and, when `require_attestation` is true, a complete `attestation` block. The manifest those steps consume is the `ghcr-release-manifest-<sha>` artifact that `publish-ghcr.yml` uploads, and its producer `create_release_manifest.py` emits exactly `schema_version`, `repository`, `commit_sha`, `created_at` and `images` — neither required member is ever present. Demonstrated rather than argued: a manifest built by the real producer and passed with the workflow's exact arguments exits **2** with `signed Cosign references are required for rollback evidence`. **This is pre-existing and A5 did not cause it.** The same input run against the pre-A5 revision (`2e9dbbc`) exits 2 with the identical message, because the Cosign requirement and the `attestation` lookup both predate A5 and the Cosign check fails first. So the production rollback path has been unrunnable, and since `rollback-production.yml` is `workflow_dispatch`-only, no CI run could ever have surfaced it. The likely root cause is that the workflow verifies signatures separately by shelling out to `cosign verify` (`rollback-production.yml:78-89`) while the verifier demands references that are never passed to it. **Resolved (2026-09-20) under two decisions.** (1) *Split pre-flight from post-deployment validation*: the workflow now validates only what exists before deployment, and a new post-deployment workflow validates the completed execution, because the record contract demands a provider operation ID, a `PASSED` healthcheck and an observed timestamp that cannot exist while the Darkube deployment is still manual. (2) *Sign the manifest with a dedicated release secret*: the publisher signs with `OKR_RELEASE_MANIFEST_ATTESTATION_SECRET`, kept separate from the provider's `OKR_SAAS_ATTESTATION_SECRET` so a repository workflow can never mint evidence attributed to an external provider. What landed: `scripts/create_release_manifest.py` gained `--require-attestation` and refuses to write an unsigned manifest; `scripts/attest_evidence.py` (new) signs derived records, because a record that gains a member after signing cannot inherit the earlier signature; both manifest steps in `rollback-production.yml` now verify Cosign *before* the evidence contract and pass every `image@digest` as `--cosign-reference`; the jq record carries `cosign_references`; the final pre-flight step validates `--approval` instead of `--record`; `rollback-execution-verification.yml` (new, `workflow_dispatch`) attaches the provider outcome and validates it with the strict record contract; `verify_release_pair.py` forwards each manifest's own references and finally has tests. The guard this row asked for now exists: `tests/test_rollback_workflow_wiring.py` parses the workflows and asserts each verifier step's arguments and environment, and it was mutation-tested rather than assumed — removing the references fails 1 test, switching the pre-flight to `--record` fails 2, and dropping `--require-attestation` from the publisher fails 3. Honest limit: the post-deployment execution outcome is operator-supplied, so its attestation records what it was given rather than proving the deployment independently. |

| F1-F4 - Read-path authorization parity | F1, F3 and F4 IMPLEMENTED; F2 Open | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `backend_app/read_query_helpers.py` (`_validate_read_scope`, `_READ_SCOPE_POLICY`, `_require_node_owner_via_https`, `_team_visible_for_scope`), `src/services/supabase_api_mode_read.py` (`scope` parameter, `_team_scope_id`), `backend_app/scope_resolution.py` (`team_id`), `src/crud_query_helpers.py` (`UnscopedNodeReadError`), `tests/test_dual_mode_parity.py`, `tests/test_crud_authorization.py` | 2026-09-21 | Found by verifying an external review that reported three unscoped read kinds; verification widened it to fourteen and moved the root cause. The HTTPS path returned at `read_query_helpers.py:271` before any TCP branch ran, and the guard that was meant to protect it enumerated the kinds it checked, covering 8 of the 26 allowed kinds. The other 18 were unchecked and 14 of them were scoped on TCP (the list below is the fourteen; the count was mis-added as "thirteen" at first and is corrected here rather than rewritten out of `48471d5`): `audit.summary`, `users.all`, `users.team_members`, `cycles.all`, `cycles.active`, `node.detect_type`, `krs.by_cycle`, `krs.needing_checkin`, `tasks.by_cycle`, `work_logs.by_task`, `experiments.for_retro_window`, `retros.team`, `alignments.context`, `mindmap.root`. The sharpest instance is `audit.summary`, which returns 403 to a non-admin on TCP and had no admin check at all over HTTPS while accepting caller-supplied `actor`, `actor_user_id` and `target_owner_id` filters. Proven fail-first rather than argued: with a non-admin scope the HTTPS path raised *nothing* for `audit.summary` and `users.all` (`DID NOT RAISE`), and 8 of 20 new cases failed before the fix. The reason it survived a file named for dual-mode parity is that the test's HTTPS half monkeypatched `read_query_via_supabase_api` to return the expected payload and then asserted that payload, over 2 of 26 kinds with an admin scope — a tautology. The guard is now deny-by-default and runs before dispatch on **both** paths, so an allow-listed kind with no declared rule is refused 403 and cannot be served unscoped by omission; the test asserts `get_read_scope_policy_kinds() == get_read_query_allowed_kinds()`. The resolved scope is passed into `read_query_via_supabase_api` instead of being discarded at `_ = actor`, `teams.all`/`teams.by_id` are scoped to the actor's team membership with the restriction pushed into the REST query rather than applied after fetching every team, and `mindmap.children` — listed as readable while implemented on neither path — was removed rather than implemented speculatively. `get_node`'s actor-optional fail-open is now `UnscopedNodeReadError` unless a caller passes `allow_unscoped=True`, with a call-site scan asserting no caller relies on it. **Reachability, established from configuration and not assumed:** the HTTPS path is *not* reachable in the enforced `single_tenant_saas` profile, because `src/saas/environment_config.py:51-67` rejects any `OKR_DATA_ACCESS_MODE` but `database` and raises when `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` or `SUPABASE_ANON_KEY` is non-empty ("HTTPS Supabase fallback is disabled"), which closes both `resolve_read_mode()` HTTPS triggers at once; the path *is* reachable in the alpha/self-hosted `supabase_api` mode, so the exposure is real there and the fix is not merely defensive. One UI consequence is recorded rather than absorbed: a **manager** opening admin mode does issue `teams.all` (`useShellAccessControl.ts:93-101` gates on `isAdmin || isManager` to feed the cycle-owner dropdown), so that request now returns only their own team. No manager-visible surface renders it — `adminTeams` appears only under `adminTab === "teams"` (`AdminModePanel.tsx:784`) and managers are forced to the `cycles` tab (`useShellAccessControl.ts:82-84`) — so no rendered behaviour changes, but the call site exists and the decision to leave it is deliberate. F2 remains open: the seven kinds that depend on `get_node(..., actor_username)` for their scope still need the `actorRequired: true` surface audited end to end. **F3 remains open, and it is the half of F1 this change does not close.** The guard enforces scope that is derivable from the request itself — admin-only kinds, and `user_id` / `username` / `manager_id` / node-owner predicates — which covers 8 of the 14. The remaining six are *classified* now but still not *filtered* over HTTPS: an in-scope request for `cycles.all`, `cycles.active`, `krs.by_cycle`, `krs.needing_checkin`, `tasks.by_cycle` or `experiments.for_retro_window` returns a wider row set over HTTPS than over TCP, because the id in the request is legitimately in scope while the rows are not. `cycles.all` was the clearest: `src/services/supabase_api_mode_read.py:303-312` selected every cycle with no scope predicate while the TCP branch filtered through `_visible_cycles_for_scope`. That is now closed too — `_apply_https_cycle_row_scope` applies the same helper and the member primary-active-cycle rule to the fetched rows, with fail-first evidence that reverting it makes the parity test report `HTTPS leaked a foreign cycle` four times while the TCP half stays green — and the filter sits in the caller rather than in the Supabase query because the member case selects the primary active cycle, which is not expressible as a PostgREST filter. The over-fetch that remains is a performance cost, not a disclosure: filtering happens in the process that already holds the rows. Narrowed twice the same day, and still open: the guard now also runs `_resolve_effective_cycle_id_for_scope` for the four cycle-naming kinds (`krs.by_cycle`, `tasks.by_cycle`, `krs.needing_checkin`, `experiments.for_retro_window`), so a cycle the actor may not use is refused 403 on both paths instead of being served over HTTPS, and `krs.needing_checkin` now authorizes its `user_id` as a username the way TCP does — a second leak found only because a test written for the cycle case failed with 400 and the 400 turned out to be the contract, not the test. Reverting just the guard turns 5 of those cases red. What remains is the goal-owner row filter for those four kinds: a member still receives sibling KRs, tasks and experiments owned by others in the same cycle over HTTPS, because their queries filter by `cycle_id` and never by `owner_ids`, and closing it requires resolving goal owners inside the Supabase implementation since that relation is absent from the fetched rows. F4 was found while scoping F3 and is now **closed**. It had been recorded as suspicion because only half of it was demonstrable at the time; fixing it produced the other half from the schema rather than from data, since `Goal.cycle_id` is `foreign_key("cycle.id")` (`src/models.py:373`) and `Objective.goal_id` is `foreign_key("goal.id")` (`:423`), so the HTTPS filter paired one table's primary key with another's. The decisive evidence was internal: the same function's fallback branch already resolved `goal` by `cycle_id`, so the embedded fast path contradicted its own sibling. Both fast paths now resolve the cycle's goals once through `_allowed_goal_ids_for_cycle` and constrain on `objective.goal_id = in.(...)`, which simultaneously supplies the ownership filter F3 needed, so `krs.by_cycle` and `tasks.by_cycle` are closed on both counts. F3 itself is now **closed**: `krs.needing_checkin` is filtered by the requested user's goals — TCP has no admin bypass for it (`src/domain/analytics.py:253`) — and additionally by `state == ACTIVE`, which the HTTPS path had also omitted; `experiments.for_retro_window` is narrowed to the actor's goals, which reproduces TCP's per-row `_authorize_goal_scoped_access` (`src/crud_experiment_helpers.py:373-384`) because `scope["owner_ids"]` is exactly "the goal's owner or that owner's manager". Reverting the source turns 4 of those cases red. The row filter for `experiments.for_retro_window` and the goal narrowing generally are applied in-process rather than as a REST predicate, because the ownership test is not expressible as one; that leaves an over-fetch, which is a performance cost and not a disclosure. Three tests had pinned `objective.goal_id == "eq.<cycle_id>"` and passed for exactly that reason; they now pin the goal's cycle, and reverting the source turns five cases red. Two of them also relied on the absence of a `scope`, so a scopeless read now fails closed and returns nothing rather than the whole cycle — a deliberate behaviour change, made consistent with `_team_scope_id`, and asserted by its own test. Stated plainly rather than rounded off: deny-by-default closes the *omission* class (a kind can no longer be served unscoped by never being registered) and refuses out-of-scope requests on both paths, and it does not by itself make those six kinds' rows agree. |
| A6b - `publish-ghcr.yml` fails closed on a secret the repository has never had | Gate IMPLEMENTED; provisioning BLOCKED on an operator | [REMAINING_ENGINEERING_PLAN.md](REMAINING_ENGINEERING_PLAN.md); `.github/workflows/publish-ghcr.yml`, `scripts/create_release_manifest.py:105-122`, `scripts/check_workflow_secret_prerequisites.py`, `docs/deploy/required-secrets.json`, `tests/test_workflow_secret_prerequisites.py`, `.github/workflows/ci.yml` (`workflow-secret-prerequisites`) | 2026-09-21 | Reported as "Assemble release manifest failed" on `main` at `b063e00`. Root cause found by bisection rather than guessed: the workflow last succeeded at `3e35315c` (2026-09-20 14:57) and has failed at the same step on every push since, beginning with `2e31480` (PR #178). The only commit in that range touching these files is `62774ef` — the A6 fix — which added `--require-attestation` and bound `secrets.OKR_RELEASE_MANIFEST_ATTESTATION_SECRET`. The repository has **zero secrets** (`gh api repos/.../actions/secrets` → `total_count: 0`), so that variable expands to empty and `scripts/create_release_manifest.py` refuses by design, exiting 1. Reproduced locally with the workflow's exact arguments over three synthetic fragments: exit 1 without the secret, exit 0 with it. **This is a defect of mine, not of the workflow.** A6 was recorded IMPLEMENTED while `publish-ghcr.yml` triggers only on push, so no pull request could observe the failure, and the test covering the wiring asserted the secret was *referenced* rather than that it *existed* — green tests certifying a workflow that cannot run. It also contradicted the A3 reasoning recorded in this same ledger, which argues that a permanently red required check teaches people to ignore required checks. The fix is split by owner. Gate half, done here: `scripts/check_workflow_secret_prerequisites.py` runs unconditionally on every pull request and requires every `secrets.X` a workflow references to be declared in `docs/deploy/required-secrets.json`, with `"post_merge_only": true` for any secret that no pull request can exercise; 21 of the 24 declared secrets are post-merge only, which is itself the finding. **Stated limit: the gate cannot verify provisioning** — reading secret metadata requires admin scope that CI must not hold — so it proves the requirement is declared and reviewed, never that a secret exists, and a green run is not evidence a workflow will succeed. Operator half, outstanding: `OKR_RELEASE_MANIFEST_ATTESTATION_SECRET` must be created, and `main` stays red until it is. Fixed alongside: `curl-retries: 10` was passed to `sigstore/cosign-installer` in six workflows although GitHub reports the action accepts only `cosign-release`, `install-dir` and `use-sudo`, so the install-retry hardening those lines advertised never took effect anywhere. |

Every item dated 2026-09-20 or 2026-09-21 above is carried in **PR #177**, **PR #178**
and **PR #179** against `main`. `IMPLEMENTED` here means the change is complete with CI
green. **Correction (2026-09-21):** this paragraph previously stated that the work was
open in PR #177 and not yet merged, and therefore that none of these rows was yet true
of `main`. That is no longer accurate and is corrected rather than left standing:
PR #178 (`2e31480`) and PR #179 (`b063e00`) have both merged, so `main` now contains
these changes. Note that neither merge was performed by this agent.

## Signed review follow-up: 2026-09-01

The runtime compatibility database is confirmed at `drop_global_cycle_index (head)`. A PostgreSQL 17-compatible backup of the live runtime database was restored successfully into an isolated rehearsal instance. P0-06 remains open only for application release rollback rehearsal and selection of a prior deployable application artifact. Shared-database tenant/RLS schema work is permanently out of scope for this product and is not a prerequisite for the dedicated single-tenant path.
## Owner decision: disposable pre-SaaS database - 2026-09-01

The runtime database has been intentionally emptied for pre-SaaS work. Application tables were truncated with cascade, the schema was retained, and no migration or dump workflow is required. Database recovery controls are deferred to the SaaS persistence phase; shared-database tenant/RLS work is permanently out of scope.
## Owner confirmation: branch promotion - 2026-09-01

The reviewed `SPA-BFF-clean` work has been promoted to the project mainline. REV-001 is resolved by owner confirmation. The promoted baseline is intentionally empty of application data and remains pre-SaaS; shared-database tenant/RLS work is permanently out of scope.
## Active handoff: pre-SaaS product baseline

The project is ready for pre-SaaS product work on the promoted mainline. The runtime database is intentionally empty. Customer isolation is provided by a dedicated application and database per customer. Shared-database tenant isolation and RLS are permanently out of scope and must not be introduced implicitly through feature implementation.
## Decision: architecture initiative handoff - 2026-09-01

The pre-SaaS architecture work is complete for its current scope and is handed off to product implementation. Further architecture changes require a concrete product requirement or an explicitly scheduled SaaS transition; speculative tenant, RLS, migration, and persistence work is out of scope.
## P0-06 disposition: deferred for disposable pre-SaaS only - 2026-09-01

The historical owner risk acceptance applied only while the database contained disposable mock data. That data has since been purged. P0-06 remains an open future SaaS feature, not a production control: the SaaS persistence entry gate must require provider-supported backup/recovery, isolated restore evidence, application rollback rehearsal, documented RPO/RTO, and an accountable operational owner before real customer data is introduced. Shared-database tenant/RLS work is permanently out of scope. The `deferred` backup configuration is permitted only for disposable synthetic environments.

## Task 5 evidence: provider-backed backup and restore operations - 2026-09-01

Task 5 is implemented without touching the live database. `BackupManager` and
`RestoreManager` use provider-issued identifiers and checksum metadata; the
local rehearsal adapter does not create application table dumps. Restore rejects
live targets before provider invocation and records operator, target, checksum
verification, and measured duration. The focused test command returned
`18 passed in 0.36s`. Backup create, verification, and restore failure states persist complete
operational metadata across manager and CLI reload. The local adapter requires explicit `--test-only`, while
the production CLI refuses to run until a real provider is configured. This
closes the implementation slice while leaving the production-provider and
production-disaster-recovery gate open as intended.

## Task 6 evidence: control-plane environment inventory and lifecycle audit - 2026-09-01

The control plane is metadata-only. Operator-authorized routes expose environment
identity, deployment profile, application version, health, database, release, and
backup metadata and record lifecycle audit events without reading or mutating
customer OKR records. Import and route boundary tests cover the separation. No
customer-domain proxying, tenant schema, RLS, migrations, or live cloud operations
are introduced by this task.

## 2026-09-01 - Task 7 Phase 1 entry-gate evidence

Task 1-6 evidence has been consolidated in [Phase 1 entry evidence](saas/phase-1-entry-evidence.md). The result is **evidence assembled, Phase 1 promotion blocked**.

- The single-tenant environment contract, profile-safe configuration, idempotent provisioning, versioned release operations, backup/restore contracts, and metadata-only control-plane inventory have focused local evidence.
- Final reported focused evidence counts are: Task 1 `22 passed`; Task 2 `27 passed`; Task 3 `14 passed`; Task 4 `13 passed`; Task 5 `18 passed`; Task 6 `34 passed` for the paired control-plane/boundary evidence.
- Provider-backed application rollback: **NOT AVAILABLE - provider/artifact not selected**.
- Provider backup, restore, measured restore timing, retention/freshness monitoring, and production RPO/RTO: **NOT AVAILABLE - provider/artifact not selected**.
- Platform/operations owner: **UNASSIGNED**.
- Real customer data remains prohibited until those provider-specific gates and application rollback evidence are complete.
- Shared-database RLS and tenant identifiers are permanently out of scope. Real-data onboarding remains blocked until the dedicated single-tenant persistence gates pass.

The phase evidence checker is executable via `just saas-evidence` and is
structured: it parses the fenced JSON evidence object and validates provider
backup identity, immutable artifact digests, rollback result, measured RPO/RTO,
and named owners rather than trusting headings or prose. It is expected to fail
while provider evidence or the named operations owner is absent. Control-plane state is durable by default at
`tmp/saas-control-plane.json`; provisioning, release, and backup operations
reconcile their metadata into that same state boundary.

## 2026-09-01 - Final blocker fix wave

The whole-plan integration blockers are addressed locally: provisioning and
control-plane persistence use one bounded crash-safe lock without deleting the
lock path; provider-returned canonical database resource IDs are persisted;
failed release and backup/restore operations reconcile degraded/failed state;
provisioning and release CLI operations require an explicit operator and record
lifecycle audit events; and control-plane read/write operations reload under
the lock so concurrent processes preserve records and events. These are local
implementation controls only. Provider-backed backup/restore, provider-backed
rollback, measured production RPO/RTO, and named operations ownership remain
intentionally blocking gates for production SaaS and real customer data.

## 2026-09-01 - Whole-plan review remediation

The evidence checker now requires a machine-readable production attestation
with provider provenance, provider-issued backup/restore identifiers, pinned
artifact digests, numeric measured rollback/RPO/RTO values, named owners, and
an HMAC-SHA256 signature verified with the configured attestation secret. The
attested values are cross-bound to the structured evidence fields. Current
evidence fails closed because those facts and the configured signature are
unavailable. Provisioning, release, backup, and restore CLIs now resolve
an authenticated principal from `OKR_OPERATOR_TOKEN` plus a credential file;
arbitrary `--operator` values and insecure defaults are rejected. Release and
backup local state persistence uses the shared crash-safe lock, and provider
database resource IDs are strictly opaque and canonical. Production SaaS and
real-data onboarding remain blocked.

## 2026-09-01 - Final trust-boundary hardening wave

Lifecycle service APIs now require an `OperatorCredential` object carrying the
authenticated principal and credential provenance; arbitrary operator strings
are rejected. The credential-file/token resolver constructs this object for
CLIs, and audit records preserve only its authenticated principal. Control-plane
initialization writes execute inside the shared crash-safe guard. No live
provider was invoked and tenant/RLS/customer-domain behavior is unchanged.

## Production persistence evidence package - blocked pending provider evidence

The repository contains the evidence shape for the controlled single-tenant
SaaS path under the repository contract in
[saas/phase-1-entry-evidence.md](saas/phase-1-entry-evidence.md). The current
record is intentionally blocked: Hamravesh/Darkube provider evidence, a named
operations owner, and a verified attestation are not present.

The approved evidence package includes the following, tied to the same
environment and customer identity:

- provider-supported backup evidence with a provider-issued backup identifier
  and successful verification
- restore evidence into a registered isolated target with a provider-issued
  restore identifier, integrity result, cleanup record, and measured duration
- documented RPO and RTO targets with numeric measured backup freshness and
  restore timing from the provider-backed drill
- a named decision owner and a named platform/operations owner, plus the
  authenticated operator for the recovery action
- immutable application release and rollback evidence with measured rollback
  duration
- explicit real-data approval and a matching production attestation signature

Operations must independently verify the provider records and rerun the
fail-closed checker with the configured attestation secret before production
promotion. Until then, no approval is active and customer-data onboarding is
not authorized. Shared
multi-tenant schema, shared-database RLS, and cross-customer data mixing remain
permanently out of scope and are not authorized by this record.

The release signoff record is preserved in [saas/release-signoff.md](saas/release-signoff.md).

## 2026-09-14 - Forwarded role-claim enforcement

The browser-facing BFF path remains the authorized trust boundary, but the backend now also verifies that any forwarded `X-OKR-Role` and `X-OKR-Roles` claims match the actor's resolved scope. This closes the remaining repo-side spoofing vector where a caller could attempt to impersonate a stronger role by sending mismatched role metadata. The change is covered by the backend ingress regression suite and is treated as a fail-closed control.

## Current status reconciliation (2026-09-02)

This section is the authoritative status summary after the repository-side architecture work.

| Area | Current status | Evidence and remaining condition |
|---|---|---|
| Backend package boundary and `app.py` retirement | `VERIFIED` | `backend_app` is the canonical API boundary, `src` owns application/domain services, and the obsolete root `app.py` facade has been removed. Import-boundary and facade migration checks are complete. |
| BFF boundary enforcement | `VERIFIED` repository-side; operational closure `OPEN` | TypeScript import and package-manifest guards, generated-contract checks, and BFF security controls are enforced. Darkube/provider deployment evidence, production measurements, and paired rollback rehearsal remain open. |
| CI path filtering | `VERIFIED` repository-side | Workflow classification and documentation-only protections are covered by contract tests. This does not replace provider-backed release evidence. |
| Deployment topology checks | `VERIFIED` repository-side; provider verification `OPEN` | Compose topology checks enforce private backend/worker/database ingress and independent services. Darkube networking, ingress, restart, and failure-isolation evidence remain open until that environment is configured. |
| SaaS isolation model | `FINAL` | Dedicated application and database per customer is the only supported model. Shared-database multi-tenancy and RLS tenant isolation are permanently out of scope. |
| Release and recovery readiness | `OPEN` | Application rollback and production backup/recovery evidence are not claimed. Database recovery remains separately deferred under explicit owner risk acceptance. |

Repository-side verification must not be interpreted as production or Darkube readiness. Provider-dependent items remain open until observed evidence is recorded.
