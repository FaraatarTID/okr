# Worklog

Documentation HQ: [README](../../../README.md)

Append-only session journal for architecture backlog execution. Newest first.
Process: [ARCHITECTURE_DELIVERY_SYSTEM.md](ARCHITECTURE_DELIVERY_SYSTEM.md).

<!-- Format:
## YYYY-MM-DD — <item>: <what>
- <changes made>
- Tests: <files added/updated>
- Next: <resumable next step>
- Blockers: <none or description>
-->

## 2026-08-31 — Monorepo and package-management tooling: workspace baseline
- Added root `pyproject.toml` with pinned Python runtime dependencies and development tooling for uv resolution, plus root `package.json` workspaces for `spa-bff` and `spa-web`.
- Generated `uv.lock` with 53 resolved packages and the root npm `package-lock.json`; existing service lockfiles and `backend_app/requirements.txt` remain unchanged for compatibility.
- Updated `ARCHITECTURE_BACKLOG.md`, `docs/architecture-status.md`, and `ENTERPRISE_RELIABILITY_ROADMAP_REWRITABLE.md` with the implemented baseline and phased follow-ups.
- Tests: not run; lock generation completed successfully.
- Next: review lockfile compatibility, migrate CI installation to workspace manifests, then add import-boundary checks.
- Blockers: none.

## 2026-08-31 — Monorepo and package-management tooling: CI and boundaries
- Migrated backend quality and SPA e2e CI jobs to `uv sync --locked --group dev` and root `npm ci` using committed workspace lockfiles.
- Added `scripts/check_import_boundaries.py` to prevent `src` from importing `backend_app` and prevent the npm workspaces from depending on one another.
- Tests: not run.
- Next: review deployment compatibility with workspace installs and measure whether incremental build caching is justified.
- Blockers: none.

## 2026-08-31 — Monorepo and package-management tooling: deployment adoption
- Migrated the Docker runtime image and deploy-build CI gate to the committed `uv.lock` dependency graph.
- Retained `backend_app/requirements.txt` as a compatibility fallback for external deployment environments that have not adopted uv.
- Tests: not run; Docker build was not executed.
- Next: measure CI build duration before deciding whether incremental Turborepo/Nx-style caching is justified.
- Blockers: none.

## 2026-08-31 — Monorepo and package-management tooling: hosted CI verified
- Hosted CI passed after the uv environment, Linux Rollup optional dependency, and hoisted Vitest/happy-dom fixes.
- Items 14 and 15 are now VERIFIED in `docs/architecture-status.md`.
- Tests: all CI tests passed per hosted-runner result.
- Next: keep external deployment-owner review and cache-performance measurement as the only follow-ups.
- Blockers: none for the reported CI failures.

## 2026-08-31 — Monorepo and package-management tooling: hosted workspace hoisting fix
- Fixed SPA web test scripts and CI commands to resolve Vitest through npm workspace `PATH` rather than requiring a package-local `node_modules/vitest` path.
- Promoted `happy-dom` to root workspace tooling so hoisted Vitest can resolve the configured test environment from a clean root install.
- This supports clean root `npm ci --include=optional` installs and prevents Linux hosted CI from failing before test discovery.
- Tests: clean root install passed; SPA web workspace tests passed with 38 files and 148 tests.
- Next: rerun hosted CI and confirm the Linux Rollup optional package resolves on the hosted runner.
- Blockers: hosted CI rerun required; root audit retains one low development-only esbuild advisory.

## 2026-08-31 — Monorepo and package-management tooling: hosted CI compatibility
- Fixed hosted CI failures after the uv migration by installing `pip-audit` and `pip-licenses` through `uv pip` instead of assuming pip exists in the uv-managed environment.
- Added the Linux Rollup native package as an explicit optional BFF dependency and enabled `npm ci --include=optional` in SPA CI jobs so Linux runners materialize the required native module.
- Tests: lockfile entries and CI configuration updated; Linux hosted-runner verification remains pending.
- Next: rerun hosted CI and confirm the Linux Rollup package and uv-installed audit tools are available.
- Blockers: hosted CI rerun required.

## 2026-08-31 — Monorepo and package-management tooling: external deployment contract
- Documented the locked uv runtime contract and compatibility fallback requirements in `DEPLOYMENT.md`, including acceptance checks for external deployment environments.
- Tests: documentation-only change; Docker build and `uv lock --check` already passed.
- Next: obtain deployment-owner confirmation for any non-Docker environment and collect hosted CI cache-hit/duration data.
- Blockers: external deployment-owner confirmation and hosted CI observations are outside the local workspace.

## 2026-08-31 — Monorepo and package-management tooling: cache measurement
- Added cache-hit reporting to the SPA quality job summary for the source-aware Next.js and BFF TypeScript build caches.
- Tests: workflow-only change; hosted CI measurement pending.
- Next: use hosted cache-hit and duration data to decide whether Turborepo/Nx task-graph orchestration is justified.
- Blockers: external deployment compatibility still requires environment-owner review.

## 2026-08-31 — Monorepo and package-management tooling: Docker validation
- Docker build passed with the uv-based runtime image: Python 3.11 image, locked dependency resolution, source copy, non-root user setup, and final image export all completed successfully.
- Tests: `docker build -f deploy/docker/Dockerfile -t okr-backend:ci .` passed.
- Next: monitor hosted CI cache hit rates and review external deployment environments for workspace-lock compatibility.
- Blockers: none for the repository Docker runtime; one low development-only root npm esbuild advisory remains documented.

## 2026-08-31 — Frontend dependency security remediation
- Upgraded `spa-web` Vitest and coverage tooling from the critical-advisory range to 4.1.11 and regenerated both SPA and root workspace lockfiles.
- Updated `spa-web/vitest.config.ts` to use the Vite/Rolldown-compatible Oxc JSX configuration.
- Upgraded Next.js to 16.3.3, kept the generated `spa-web/tsconfig.json` updates, and added a scoped BFF esbuild override.
- Tests: SPA tests 148 passed; SPA typecheck and production build passed; BFF tests 65 passed and build passed; service-local audits report zero vulnerabilities; root audit retains one low development-only esbuild advisory.
- Next: monitor the remaining nested esbuild advisory and evaluate a compatible Vitest/Vite upgrade when available.
- Blockers: no critical or high findings remain; the root low advisory conflicts with SPA web's Vite 8 esbuild constraint.

## 2026-08-31 — Monorepo and package-management tooling: CI verification
- Full Python suite passed: `592 passed, 8 skipped`; mypy passed for 36 files; migration lint and import-boundary gates passed.
- SPA verification passed: BFF tests 65 passed, BFF build passed, SPA web tests 148 passed, SPA web typecheck passed, and production build passed.
- `uv lock --check` passed. Docker build was blocked by local Docker Desktop permissions; root `npm ci` was blocked by sandboxed registry access and npm then reported `Exit handler never called`. Service-local locked installs were restored successfully.
- Next: monitor hosted CI cache hit rates and review whether the remaining deployment environments can consume workspace locks.
- Blockers: local Docker Desktop and registry access permissions.

## 2026-08-31 — Monorepo and package-management tooling: incremental cache baseline
- Enabled source-aware GitHub Actions caches for `spa-web/.next/cache` and BFF TypeScript incremental state, keyed by workspace dependencies and relevant service sources/configuration.
- Added TypeScript incremental compilation metadata under `spa-bff/.cache`.
- Tests: not run; CI cache effectiveness is pending a workflow run.
- Next: compare CI build duration and cache-hit rates before considering Turborepo/Nx task-graph orchestration.
- Blockers: none.

## 2026-08-31 — Monorepo and package-management tooling: workspace build warning
- Configured `spa-web/next.config.ts` with an explicit repository tracing root after the root workspace lockfile caused Next.js to warn about multiple lockfiles.
- Tests: SPA production build passed after dependency restoration; Docker build remains blocked by local Docker Desktop permissions.
- Next: retry root npm workspace installation with a task-local cache and monitor CI cache effectiveness.
- Blockers: none.

## 2026-08-31 — Cycle inspection: repaired manager goal assignment
- Investigated the live Atlas report that `Cycle Manager#1` was empty while its items appeared under `Q1 2026`. The running stack was confirmed to use Supabase HTTPS mode, not the empty local Compose PostgreSQL volume.
- Verified that cycle 4 (`Cycle Manager#1`) was active but had no goals. Goal 8 (`PMO`), owned by `manager1`, was incorrectly persisted with `cycle_id=1` (`Q1 2026`). The goal was reassigned to `cycle_id=4`; its existing objective, key result, and task remained attached through the goal hierarchy.
- Confirmed the live Atlas snapshot for cycle 4 returns goal 8 with 1 objective, 1 key result, and 1 task. The frontend cycle-transition fix also clears stale snapshot state before loading a newly selected cycle.
- Tests: focused Atlas tests 13 passed; frontend typecheck passed; production build passed; live Supabase snapshot and persisted goal assignment checks passed.
- Next: add a data-integrity check or administrative repair workflow for goals whose owner and cycle ownership disagree.
- Blockers: none.

## 2026-08-26 — Item 7: data-access strategy contract implemented
- Added `src/services/data_access_strategy.py` with the minimal `IDataAccessStrategy` protocol, typed result/error metadata, callable-backed direct database and Supabase API adapters, and a mode-to-adapter factory.
- Existing public CRUD and Supabase helper signatures remain unchanged; the contract wraps dispatchers without changing production routing or fallback behavior.
- Tests: `tests/test_data_access_strategy.py`; focused resolver/parity set: 25 passed; full Python suite: 585 passed / 8 skipped.
- Next: item 7 verification drill, then item 8 request-scoped strategy selection.
- Blockers: local Compose smoke remains blocked by Docker Desktop Linux engine availability.

## 2026-08-29 — Item 8: request-scoped strategy selection closed
- Added request-local data-access context state for actor, request/correlation IDs, preferred strategy, and fallback policy using `ContextVar`, with token-based cleanup and nested-context isolation.
- Platform read routes now use the effective request-aware resolver; existing TCP-primary/HTTPS-fallback behavior and mutation fail-closed dispatch remain unchanged.
- Fixed resolver monkeypatch leakage in ritual snapshot tests and removed a local `timedelta` import that broke Supabase audit-summary reads through Python name shadowing.
- Tests: focused request-context/platform-read checks 14 passed; full Python suite 587 passed / 8 skipped; Pylance diagnostics clean for touched Python files.
- Next: item 9 strategy and fallback observability.
- Blockers: none.

## 2026-08-29 — Item 9: strategy observability foundation
- Extended bounded in-memory API metrics with optional strategy and fallback-reason counters while preserving existing metric callers.
- Request middleware now carries request-local access context metadata into the observability boundary, including exception responses.
- Tests: backend observability checks 5 passed; exception-envelope plus observability checks 6 passed; full Python suite 587 passed / 8 skipped.
- Next: populate effective strategy and fallback reason from resolver outcomes, then add item 9 verification evidence.
- Blockers: item 9 remains open until attribution is complete.

## 2026-08-29 — Item 7: verification drill closed
- Docker was available and the resilience verification completed successfully: resilience pytest checks passed and the full-stack Compose smoke passed.
- The initial run exposed hard-coded service container names that collided with the running deployment stack. Removed those names from `deploy/docker/docker-compose.yml`; Compose project names now provide isolation while preserving service discovery.
- Item 7 is CLOSED in `docs/architecture-status.md`; item 8 is next.
- Blockers: none for the completed item.

## 2026-08-26 — Verification drills: P0 backlog and per-manager cycles closed
- User-confirmed completion of the break-a-payload OpenAPI drill, signing-key rotation rehearsal, full-day SLO measurement, dead-letter inject/discover/retry drill, and Phase E3 two-manager activation/isolation drill.
- Updated `docs/architecture-status.md` to close items 1, 2, 3, 4, and 6 with dated evidence and retrospective notes.
- Closed `docs/PLAN_PER_MANAGER_ACTIVE_CYCLES.md`; manager AI cycle-context selection was corrected and included in the closure note.
- Item 5 complete: generated the BFF allowlist from explicit route policy metadata validated against OpenAPI; added deterministic drift gates and regression tests. Verification: policy check passed, 2 Python tests passed, and all 65 BFF tests passed.
- Blockers: none.

## 2026-08-26 — CI: insert payload schema contract gate wired
- `ci.yml` (backend-quality job): added "Insert Payload Schema Contract Gate (offline)" step running `check_insert_contract.py --from-baseline` after the quality baseline gate.
- `docker-deploy.yml` (build job): same gate before image build, so a drifted schema can't produce a deployable image.
- Both use offline baseline mode — no DB credentials needed in CI. Gate command verified locally (exit 0); related tests 8/8 pass.
- Next: none for this item; drill remains Phase E3 verification.
- Blockers: none.

## 2026-08-26 — Schema contract check: insert payloads vs. DB schema (follow-up)
- New `scripts/check_insert_contract.py`: cross-checks `_REQUIRED_INSERT_COLUMNS` against the actual schema in two modes — live DB (`OKR_DATABASE_URL`, authoritative) or `--from-baseline` (parses squashed baseline DDL; runs offline/CI). Catches drift in BOTH directions: stale map entries (column became nullable/gained default) and missing entries (new 23502 landmines).
- Immediate payoff: the checker found real drift on first run — baseline DDL lacked `DEFAULT 1` for `user.token_version` (present live). Fixed in `baseline_2026_08_26_schema.py`; also added `role` to the required-columns map (always sent, NOT NULL, no default live).
- New `tests/test_insert_contract_checker.py` (5 tests): baseline parsing, known column semantics, pass case, missing-entry detection, stale-entry detection.
- Full suite: 573 passed / 8 skipped.
- Next: optionally wire `check_insert_contract.py --from-baseline` into CI quality gate.
- Blockers: none.

## 2026-08-26 — Tests: API-mode insert payload regression coverage
- Gap analysis: 573 tests missed the user-create bug because every endpoint test mocks the create helpers (`test_create_user_endpoint_parses_role_and_team` fakes `create_user`; dual-mode parity test fakes BOTH handlers and only compares routing). The real payload-building code was never exercised.
- New `tests/test_api_mode_insert_payloads.py`: captures REAL payloads from `create_user_via_supabase_api`, `create_team_via_supabase_api`, `start_timer_via_supabase_api` (only `_rest_insert` transport is faked) and asserts all NOT NULL / no-default columns are present. Also asserts role stored uppercase (matches live `userrole` enum).
- Verified against live schema: `token_version` has DB default `1` (not required); `created_at`/`is_active`/`must_change_password` have none.
- Full suite: 568 passed / 8 skipped.
- Next: consider a contract test that diffs `_REQUIRED_INSERT_COLUMNS` against information_schema on deploy.
- Blockers: none.

## 2026-08-26 — Bugfix: user create 400 "null value in column created_at"
- Root cause (captured via raw PostgREST probe): live DB `user.created_at` is NOT NULL with **no server default**; the ORM `default_factory` is invisible to PostgREST, so inserts without explicit `created_at` fail with 23502. Same latent issue for `team.created_at`.
- Fix: `create_user_via_supabase_api` and `create_team_via_supabase_api` now send `created_at` explicitly (`_utc_now_iso()`).
- Note: the earlier "Internal server error" masked this — first fix (missing import) revealed the real 400 underneath.
- Tests: 49 passed (user/team subset). Rebuilt backend-api + worker.
- Follow-up: consider adding DB-level defaults (`DEFAULT now()`) for created_at columns in a future migration so all insert paths are safe.
- Next: user re-test manager creation via UI.
- Blockers: none.

## 2026-08-26 — UI: admin panel tab selection styling + per-tab status message
- Tab buttons now render from a single `ADMIN_TABS` list; the active tab gets an accent ring + bold + full opacity, inactive tabs dimmed (opacity 0.55) with `aria-pressed` for accessibility.
- Fixed lingering "Cycle activated." / other status banners across tabs: `AdminModePanel` clears `adminCycleMessage` on tab change (new `setAdminCycleMessage` prop wired from AtlasShell).
- Tests: typecheck clean; 143/143 frontend tests pass. spa-web container rebuilt.
- Next: user visual verification.
- Blockers: none.

## 2026-08-26 — Bugfix: user create 500 in supabase_api mode
- Root cause: `api_create_user` (main_mutation_handlers.py) called `_resolve_backend_main().create_user_via_supabase_api`, but `backend_app/main.py` never re-exported that name from `src.services.supabase_api_mode` → AttributeError → 500. All sibling handlers import their functions directly; this one went through the main module.
- Fix: added `create_user_via_supabase_api` to the `supabase_api_mode` import block in `backend_app/main.py`.
- Tests: 86 passed / 1 skipped (user/admin subset). Rebuilt backend-api + worker; attribute verified inside container.
- Next: user re-test manager creation via UI.
- Blockers: none.

## 2026-08-26 — Bugfix: fn_activate_cycle 400 "column updated_at does not exist"
- Root cause: the migration squash (baseline_2026_08_26) recreated tables/indexes but NOT the RPC functions, so the pre-squash `fn_activate_cycle` body survived on live Supabase. That old body (a) referenced a dropped `updated_at` column → 400 on every activation, and (b) deactivated ALL cycles globally instead of same-owner only — violating the per-manager model.
- Fix: `CREATE OR REPLACE FUNCTION fn_activate_cycle` on live Supabase with per-owner body (`owner_manager_id IS NOT DISTINCT FROM` target's owner; `deactivated_count` scoped to same owner). No `updated_at` references remain.
- Verified in rolled-back transaction: activating Q1 2026 succeeds, deactivates only same-owner active cycle, live data untouched.
- Lesson: the squashed baseline must include RPC DDL for fresh installs — follow-up needed to add `fn_activate_cycle` + `fn_ritual_snapshot` to `baseline_2026_08_26_schema.py`.
- Next: user re-test cycle activation via UI.
- Blockers: none.

## 2026-08-26 — Deploy: container rebuild + Supabase alembic re-stamp (Phase E2)
- Rebuilt all containers (`docker compose -p docker ... up -d --build`); spa-web/spa-bff/api/worker on new images.
- Worker crash loop root cause: live Supabase `alembic_version` still pointed at pre-squash head `b5c6d7e8f9a0`, deleted by the migration squash. Note: containers run in supabase_api mode against the LIVE Supabase DB, not the local okr-postgres.
- Fix: stamped Supabase `alembic_version` → `baseline_2026_08_26` (the revision ID inside the baseline file; the filename suffix `_schema` is NOT the id). No DDL executed; schema was already current.
- Verified: worker up with restartCount=0, API healthz ok (`dead_jobs: 0`), all 5 containers stable.
- Next: Phase E3 verification drill (two managers, distinct active cycles, member isolation).
- Blockers: none.

## 2026-08-26 — Per-manager active cycles: Phase C/D complete (manager Cycles panel)
- Frontend: managers can enter admin mode restricted to the Cycles panel (`useShellAccessControl` forces `adminTab="cycles"`; `AdminModePanel` renders tab buttons for admins only, "Admin or manager role required." gate).
- Ownership guards in `useAdminActions`: `canManageCycles` (admin||manager), `ownsCycle`, `canMutateCycle`; create/activate gated on `canManageCycles`; activate/deactivate/delete of admin-owned cycles blocked for managers ("This global cycle is managed by an administrator."); ownership changes remain admin-only.
- Tests: updated `useShellAccessControl.test.ts` (isManager + setAdminTab harness), `useAdminActions.test.ts` (isManager), `AdminModePanel.test.tsx` (isManager prop, new gate copy).
- Validation: spa-web typecheck clean; 143/143 frontend tests pass; backend 565 passed / 8 skipped.
- Docs: PLAN_PER_MANAGER_ACTIVE_CYCLES.md → IMPLEMENTED (deploy+drill pending); architecture-status item 6 → IMPLEMENTED.
- Next: rebuild containers (`docker compose -p docker -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env up -d --build`), then Phase E3 verification drill (two managers, distinct active cycles, member isolation).
- Blockers: none.

## 2026-08-26 — Migration squash + per-manager active cycles (Phase A/B)
- Squashed all 34 migrations into single baseline `baseline_2026_08_26_schema` (generated via `scripts/generate_baseline_migration.py` from models; Postgres DDL embedded, SQLite falls back to metadata.create_all). DB data was mock/disposable per user approval.
- Per-manager active cycles: `ux_cycle_owner_active` partial unique index (one active cycle per owner_manager_id) replaces global `ux_cycle_single_active`. Model updated to match.
- Scope logic: `_pick_primary_active_cycle` prefers manager-owned over global; member resolution = manager's active cycle, else global. Both scope_resolution.py and main_runtime_helpers.py copies updated.
- Removed 6 obsolete migration-specific tests (audit backfill, goal cutover, worklog heal, auth throttle creation, integrity tolerance) — end-state covered by bootstrap/adopt tests.
- Fixed verify_ops01_readiness.py to accept baseline schema as growth-index source post-squash.
- Validation: Python 570→565 passed / 8 skipped (net: removed 6 squashed-migration tests, added 5 per-owner invariant tests); ruff/helper-integrity/ops01/docs gates green; single alembic head.
- Next: Phase C frontend (Cycles panel for managers), then rebuild containers.
- Blockers: none.

## 2026-08-26 — Reverted manager self-service cycle creation
- Decision: letting managers create cycles was a source of chaos with no benefit; cycle governance returns to admin-only.
- Removed: Atlas "+ New cycle" section (AtlasModeControlsPanel), atlasCreateCycle* state/handler in useAdminActions, related AtlasShell wiring.
- Kept: read-only active-cycle display, merged cycles.all+cycles.active hydration, last-active deactivation guard, DB unique index. Backend permission unchanged (managers could still technically call the API; UI no longer exposes it).
- Tests: spa-web 143/143, typecheck clean.
- Next: rebuild spa-web container to deploy the revert.
- Blockers: none.

## 2026-08-26 — Cycle handling hardening (all 7 review recommendations)
1. DB invariant: partial unique index `ux_cycle_single_active` (migration z3a4b5c6d7e8) + model registration; auto-heals duplicate-active data before index creation.
2. Atomic activation: `fn_activate_cycle` RPC (migration a4b5c6d7e8f9) used by API-mode update path; 42883-only fallback to legacy two-call sequence; heads merged via b5c6d7e8f9a0.
3. Last-active deactivation guard: backend ValueError in ORM + Supabase paths; UI blocks with clear message in useAdminActions.
4. Cache: `_CachedCallable` now has TTL (5s) + distributed invalidation awareness (`check_distributed_cache_staleness` consulted per call).
5. Inactive-cycle banner: admins/managers see a warning when viewing a non-active cycle.
6. Consolidated cycles fetching: new `useCyclesSource` hook (cycles.all + cycles.active parallel, merged); AtlasShell and admin actions share it.
7. Tests: `tests/test_cycle_db_invariant.py` (4: DB rejection, inactive-multiple OK, deactivate-then-activate, last-active guard), `tests/test_cycle_single_active.py` extended to 5 (RPC-present, RPC-fallback paths).
- Validation: Python 570 passed / 8 skipped; spa-web typecheck clean, 143/143 tests; single alembic head b5c6d7e8f9a0; drift gate green after regenerating openapi.json.
- Next: rebuild containers to deploy; run the 4 pending verification drills.
- Blockers: none.

## 2026-08-25 — Bugfix: multiple active cycles invariant violation
- Root cause: neither the ORM nor Supabase-API cycle create/update paths enforced single-active; activating a new cycle left old ones active, and Atlas top bar (cycles.active) showed a stale cycle.
- Data repair: verified live Supabase state — only cycle 2 (Q3-2026) is active; cycle 1 already deactivated by user's PATCHes. No data corruption in current state.
- Fix: bulk-deactivate all other cycles before activation in both paths (`crud_cycle_helpers.py` ORM + `supabase_api_mode_operations.py` API mode). Deactivation does not touch other cycles.
- Tests: `tests/test_cycle_single_active.py` (3 cases: activate deactivates others, deactivate doesn't touch others, create-with-active deactivates existing). Full suite 564 passed / 8 skipped. Deployed to container (verified lines 646/679).
- Next: user re-test in UI; resume verification drills.
- Blockers: none.

## 2026-08-25 — Bugfix: fractional duration_minutes in Supabase API mode
- Root cause: `stop_timer_via_supabase_api` stored raw float minutes (e.g. 0.2237...) while the ORM path stores whole minutes (`int(elapsed/60)`).
- Fix: round to whole minutes in the API-mode stop path for parity; repaired the existing fractional row in Supabase (1 row fixed).
- Tests: `tests/test_timer_stop_rounding.py` (2 cases: rounds down to int, never negative). Container rebuilt and fix verified deployed.
- Next: resume verification drills.
- Blockers: none.

## 2026-08-25 — Bugfix: timer start 400 in Supabase API mode
- Root cause: `work_log.duration_minutes` is NOT NULL with no DB default; ORM applies Python-side 0.0 but PostgREST inserts bypass it → 23502 not-null violation surfaced as "timer.start/insert: 400".
- Fix: include `duration_minutes: 0` explicitly in the insert payload (`supabase_api_mode_operations.py`).
- Verified live against Supabase (insert succeeded, probe row cleaned up).
- Tests: `tests/test_timer_start_api_mode.py` (2 cases: payload includes duration_minutes; active work_log reuse skips insert). Full suite 559 passed / 8 skipped.
- Next: stack restart to pick up all sprint changes, then run verification drills.
- Blockers: none.

## 2026-08-25 — Item 0: SLO probe completion + first generated-type adoption
- `scripts/slo_probe.py` now measures all 5 SLOs: added read/query p95 (SLO-2), mutation error rate via weekly-plans upsert (SLO-3), job queue lag with marker-job polling (SLO-4), snapshot median via ritual.snapshot (SLO-5). Session-cookie login helper added; unauthenticated probes skip gracefully with explicit detail.
- Generated-type adoption: `spa-web/src/lib/api/jobs.ts` now aliases `AsyncJobView = BackendJobView` (from generated schema); hand-written duplicate removed from `types.ts`. Typecheck, tests (143), and build all green.
- Tests: Python full suite 557 passed / 8 skipped.
- Next: run the 4 live verification drills after stack restart → CLOSE ledger items; then item 5 (allowlist generation).
- Blockers: none.

## 2026-08-25 — Sprint close: docs revision + P0 backlog execution
- Revised ARCHITECTURE.md (stale data-access/security/jobs sections; new Contract Governance + Contributor Decision Guide sections); added rotation keys to CONFIG_REFERENCE EN+FA.
- Sprint totals: 4/4 P0 backlog items IMPLEMENTED; full suite 557 passed / 8 skipped; spa-web 143 passed; all CI gates green locally.
- Next: run the 4 live verification drills after a stack restart, then close items in the ledger.
- Blockers: none.

## 2026-08-25 — Item 1: OpenAPI codegen + CI drift gate
- `scripts/export_openapi.py` exports FastAPI schema (46 paths, OpenAPI 3.1) to `spa-web/src/lib/api/openapi.json`.
- `scripts/check_openapi_drift.py` CI gate: regenerates in-memory, fails with added/removed/changed path diff on drift. Wired into ci.yml after Docs HQ Link Check.
- `openapi-typescript` devDependency; `npm run gen:api` generates `spa-web/src/lib/api/generated/schema.d.ts` (~3.7k lines).
- Typed helper `spa-web/src/lib/api/backend-schema.ts` (JobView, Healthz types + parseHealthz) with 4 vitest cases.
- Tests: spa-web 143 passed (38 files); Python full suite 557 passed, 8 skipped.
- Next: verification drills for all 4 items (needs stack restart): rotation rehearsal, DLQ inject/retry, one-day SLO run, break-a-payload drift drill.
- Blockers: none.

## 2026-08-25 — Items 2/3/4: key rotation, SLOs, dead-letter jobs
- Item 2 (key rotation): `x-okr-key-id` header + overlap verification in `backend_app/security.py`; new settings `OKR_BACKEND_SIGNING_SECRET_PREVIOUS` / `OKR_BACKEND_SIGNING_KEY_ID`; BFF sends key ID (`signing.ts`, `config.ts`, `proxy.ts`, `server.ts`); runbook added to `DEPLOYMENT.md`.
- Item 3 (SLOs): SLO table (5 objectives + error-budget policy) in `docs/OBSERVABILITY_AND_RUNBOOKS.md`; probe script `scripts/slo_probe.py` (login p95, healthz, dead-job visibility).
- Item 4 (dead-letter): `GET /v1/jobs/dead` (admin), `POST /v1/jobs/{id}/retry`, `count_dead_jobs()` in healthz; allowlist + auth-matrix updated.
- Tests: `tests/test_signing_key_rotation.py` (8), `tests/test_dead_letter_jobs.py` (6); full suite 557 passed, 8 skipped. BFF suite 65 passed.
- Next: restart stack; run verification drills per ARCHITECTURE_DELIVERY_SYSTEM.md (rotation rehearsal, DLQ inject/retry cycle, one-day SLO measurement); then item 1 (OpenAPI codegen).
- Blockers: none.

## 2026-08-31 — Frontend migration: generated read contracts

- Migrated the Atlas snapshot and discriminator-based read-query request bodies to generated OpenAPI contracts via `backend-schema.ts`.
- Added a permissive typed `ReadQueryResponse` envelope for key results, weekly plans, retrospectives, work logs, and experiments; unknown fields remain preserved for other query kinds and forward compatibility.
- The frontend read-query client now returns the generated response contract instead of `Record<string, unknown>`.
- Updated the backlog and status ledger to record the completed request/response adoption slice.
- Tests: not run in this change; hosted CI was already confirmed green before this focused contract migration.
- Next: remove remaining local response casts in `useAtlasModeData` as each read kind is migrated to the generated section types.
- Blockers: none for the common response envelope.
## 2026-08-31 — Frontend migration: mode-data type cleanup

- Replaced duplicated weekly-plan, work-log, key-result, retrospective, and experiment read models in `useAtlasModeData` with generated OpenAPI response-section types.
- Removed casts from the weekly, daily, and ritual read paths; timeline-specific adaptation remains intentionally local because its query response is not yet part of the typed envelope.
- Tests: not run in this change; hosted CI was already confirmed green before this focused type cleanup.
- Next: migrate the remaining timeline and secondary read-query response sections when their backend schemas are added.
- Blockers: none for the migrated mode-data paths.
## 2026-08-31 — Frontend migration: timeline response typing

- Added a typed timeline task section to the backend read-query response envelope, including the nested key-result/objective/goal context used by the SPA timeline.
- Removed the remaining timeline task and work-log casts from `useAtlasModeData`.
- Regenerated the OpenAPI and TypeScript artifacts.
- Tests: not run in this change; hosted CI was already confirmed green before this focused type cleanup.
- Next: type secondary read-query sections as their frontend consumers are migrated.
- Blockers: none for the timeline path.
## 2026-08-31 — Frontend migration: shared resource response typing

- Added generated read-response sections for users, teams, and cycles by reusing the existing backend mutation-view schemas.
- Removed local casts from cycle queries and admin user/team loading.
- Regenerated the OpenAPI and TypeScript artifacts.
- Tests: not run in this change; hosted CI was already confirmed green before this focused type cleanup.
- Next: review remaining compatibility casts in secondary admin and node-read consumers.
- Blockers: none for shared resource reads.
## 2026-08-31 — Frontend migration: secondary work-log consumers

- Replaced duplicated work-log models and response casts in inspector history and report generation with the generated read-query work-log type.
- Kept alignment, mindmap, audit, and other open-ended read payloads explicit until their backend response schemas are modeled.
- Tests: not run in this change; hosted CI was already confirmed green before this focused type cleanup.
- Next: close the frontend migration slice after the remaining open-ended payloads receive deliberate schemas or are formally deferred.
- Blockers: none for work-log consumers.
## 2026-08-31 — Frontend contract CI repair

- Fixed the generated-type barrel export for `ReadQueryUser` and `ReadQueryTeam`.
- Fixed the Atlas request body to always provide the generated contract's required `include_analysis` field.
- Regenerated the OpenAPI JSON and TypeScript declarations after the backend response-schema additions.
- Addressed the reported OpenAPI drift and SPA build type errors at source.
- Tests: not rerun after the repair; the next hosted CI run should verify drift, typecheck, and build together.
- Blockers: none known for the reported failures.
## 2026-08-31 — Read-query cycle response regression fix

- Fixed the generic read-response contract to allow nullable cycle dates, matching existing cycle serializers and fixtures without weakening mutation response contracts.
- Regenerated OpenAPI and TypeScript declarations.
- Verification: focused cycle read test passed; OpenAPI drift check passed.
- Blockers: none for the reported CI failure.
